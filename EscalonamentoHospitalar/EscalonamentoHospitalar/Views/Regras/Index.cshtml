@model IEnumerable<EscalonamentoHospitalar.Models.Regra>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegrasEscalonamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Numero)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RegrasEscalonamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Numero)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RegraId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RegraId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RegraId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
