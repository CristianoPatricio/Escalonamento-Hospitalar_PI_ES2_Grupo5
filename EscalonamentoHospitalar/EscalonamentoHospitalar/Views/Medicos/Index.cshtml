@model IEnumerable<EscalonamentoHospitalar.Models.Medico>

@{
    ViewData["Title"] = "Index";
}

<h2>Lista de Médicos</h2>

<p>
    <a asp-action="Create">Inserir novo médico</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Código
            </th>
            <th>
                Nome
            </th>
            <th>
                Email
            </th>
            <th>
                Contacto
            </th>
            <th>
                Nº Cartão de Cidadão
            </th>
            <th>
                Data de Nascimento
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroMecanografico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contacto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data_Nascimento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MedicoId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.MedicoId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.MedicoId">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
