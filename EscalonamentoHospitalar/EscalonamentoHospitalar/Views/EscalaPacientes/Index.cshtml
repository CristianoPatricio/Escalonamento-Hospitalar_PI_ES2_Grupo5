@model IEnumerable<EscalonamentoHospitalar.Models.EscalaPaciente>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tratamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Paciente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tratamento.DuracaoCiclo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Paciente.Contacto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EscalaPacienteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EscalaPacienteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EscalaPacienteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
