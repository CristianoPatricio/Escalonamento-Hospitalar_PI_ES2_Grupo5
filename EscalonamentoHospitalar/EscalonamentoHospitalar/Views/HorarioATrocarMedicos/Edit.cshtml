@model EscalonamentoHospitalar.Models.HorarioATrocarMedico

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>HorarioATrocarMedico</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="HorarioATrocarMedicoId" />
            <div class="form-group">
                <label asp-for="HorarioMedicoId" class="control-label"></label>
                <select asp-for="HorarioMedicoId" class="form-control" asp-items="ViewBag.HorarioMedicoId"></select>
                <span asp-validation-for="HorarioMedicoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
